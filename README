Prototype: Render a variety of domain models into a common HTML template (i.e. a partial), although each
domain model has different properties.
Implemented by extening the rendering object (which is the data provider for the template) selectively at runtime.